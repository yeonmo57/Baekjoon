#include<iostream>

using namespace std;

int t, n, m;

int main() {
	cin >> t;
	while (t--) {
		cin >> n >> m;
		for (int i = 0; i < m; i++) {
			int a, b;
			cin >> a >> b;
		}
		
		cout << n - 1 << endl;
	}
}